//Calcular el área de un trángulo

let base = prompt("Introduce el valor de la base");

let altura = prompt("Introduce el valor de la altura");

let area = (parseFloat(base) * parseFloat(altura)) / 2;

alert(`Base:${base} \n Altura:${altura} \n Area:${area}`);